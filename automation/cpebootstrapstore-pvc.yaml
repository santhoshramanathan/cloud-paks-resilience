apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cpebootstrapstore-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: cpebootstrapstore-pv
  storageClassName: cpebootstrapstore-pv
